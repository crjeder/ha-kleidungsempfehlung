## Session: 2026-03-15 (session 1)
### Task: Set up automated testing environment
### Final state: validate.sh exits 0, sensor live with clo=1.68, PMV=-2.01 (known issue at the time)

---

## Session: 2026-03-15 (session 2)
### Task: CLAUDE.md optimization + Engine PMV=-2.01 investigation

### What was done:
1. **Optimized CLAUDE.md** — removed boilerplate intro, fixed heading levels, trimmed ILP formulation, deduped testing section, fixed "Larning" typo.
2. **Investigated PMV=-2.01** — traced root cause to two bugs in `optimize_ensemble` heuristic init:
   - **Hardcoded item IDs**: `undershirt_sleeveless`, `boxer_synthetic`, `pants_standard` are not present in the HA test inventory. Phantom entries blocked layer positions, contributed 0 CLO, couldn't be upgraded → `achieved_clo=1.68` at 10°C.
   - **Fit violation**: `pants_standard` (fit=outer) placed at `legs[1]` (mid layer). Outer items accumulated at wrong layer positions.
3. **Fixed heuristic init** in both `engine.py` files — now picks lightest available item per slot/fit from the actual inventory, no hardcoded IDs.
4. **Added `thermal_leggings`** (fit=mid, clo=0.40) to `inventory.json` — fills inventory gap; ILP now achieves 3.607 CLO at -10°C/5m/s (vs 3.32 before).

### Final state:
- No fit violations from either solver
- HA test inventory: heuristic clo=1.87, ILP clo=2.00 (vs 1.68 before)
- Full inventory at -10°C/5m/s: both solvers now agree at 3.607 CLO, PMV=-1.94 (vs -2.26 ILP / -2.07 heuristic before)

### Next session ideas:
- Add unit tests for `_pmv_ppd_fanger()` against ISO 7730 Annex D reference values
- Verify `sensor_gender` and `sensor_age` stubs are wired up in sensor.py
- Add multi-scenario validation to validate.sh

---

## Session: 2026-03-15 (session 3)
### Task: Eliminate engine.py code duplication (engine-single-source)

### What was done:
- Replaced root `engine.py` (~600 lines, identical copy) with a 13-line re-export shim
- Shim loads `custom_components/kleidungsempfehlung/engine.py` directly via `importlib` (bypasses `__init__.py` which has HA-only `voluptuous` dependency)
- `main.py` (CLI) unchanged — imports still resolve transparently through the shim
- HA integration completely unaffected (uses relative imports, never touches root file)
- validate.sh exits 0, all checks pass

### Final state:
- Single source of truth: `custom_components/kleidungsempfehlung/engine.py`
- Root `engine.py` = shim only

### Next session ideas:
- Add unit tests for `_pmv_ppd_fanger()` against ISO 7730 Annex D reference values
- Verify `sensor_gender` and `sensor_age` stubs are wired up in sensor.py
- Add multi-scenario validation to validate.sh
